# Local — copy to client/.env
VITE_API_URL="http://localhost:3001/api"

# Production (Vercel Environment Variables) — your Render API base + /api
# VITE_API_URL=https://YOUR-SERVICE.onrender.com/api

